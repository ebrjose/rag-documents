# Copia este archivo a `.env` y ajusta los valores.

# URL del backend FastAPI
VITE_API_BASE_URL=http://localhost:8000

# Si es `true`, el frontend usa mocks (lib/mocks.ts) en vez de llamar al backend.
# Útil mientras el backend no esté listo (ADR-013, desarrollo API-first).
VITE_USE_MOCKS=true
